{
  "services": {
    "server": {
      "root": "backend",
      "entrypoint": "index.ts",
      "buildCommand": "npm --prefix ../frontend ci && npm --prefix ../frontend run build && npm ci && npm run build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "type": "service",
        "service": "server"
      }
    }
  ]
}
