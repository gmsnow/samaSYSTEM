{
  "builds": [
    {
      "src": "backend/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/index.ts" }
  ],
  "buildCommand": "npm ci && npm run build"
}
