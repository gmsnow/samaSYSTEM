{
  "services": {
    "server": {
      "root": "backend",
      "entrypoint": "index.ts",
      "buildCommand": "ls ../frontend && npm ci && npm run build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "type": "service",
        "service": "server"
      }
    }
  ]
}
