{
  "services": {
    "server": {
      "root": "backend",
      "entrypoint": "index.ts",
      "buildCommand": "cd ../frontend && npm ci && npm run build && cd ../backend && npm ci && npm run build && mkdir -p dist/public && cp -r ../frontend/dist/. dist/public"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "type": "service",
        "service": "server"
      }
    }
  ]
}
